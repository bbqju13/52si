<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <mapper namespace="com.myweb.www.repository.ReservationDAO">
    <insert id="calender">
    	INSERT INTO calender (date, time, content)
		VALUES (#{date}, #{time}, #{content})
    </insert>
    </mapper>